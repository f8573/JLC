/**
 * CUDA backend namespace for GEMM.
 *
 * <p>Current implementation remains in legacy compute classes and is accessed
 * through the canonical {@code net.faulj.kernels.gemm.Gemm} facade.</p>
 */
package net.faulj.kernels.gemm.backend.cuda;
